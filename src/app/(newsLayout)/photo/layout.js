export const metadata = {
    title: 'Photos | The Universal News',
    description: 'Discover Opportunities: Your Next Career.',
  }
const PhotoLayout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default PhotoLayout;