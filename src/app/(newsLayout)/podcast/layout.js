export const metadata = {
    title: 'Podcast | The Universal News',
    description: 'Podcast: Listen, Learn, Be Inspired.',
  }
  
const PodcastLayout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default PodcastLayout;