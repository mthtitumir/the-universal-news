export const metadata = {
    title: 'Travel | The Universal News',
    description: 'Explore the World, Create Memories',
  }
const TravelLayout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default TravelLayout;