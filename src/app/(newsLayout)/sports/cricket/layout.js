export const metadata = {
    title: 'Cricket | The Universal News',
    description: 'Cricket: Thrills, Bat, Ball, Victory!',
  }
  
const CricketLayout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default CricketLayout;