export const metadata = {
    title: 'Videos | The Universal News',
    description: 'Explore the World, Create Memories',
  }
const VideosLayout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default VideosLayout;