export const metadata = {
    title: 'Book | The Universal News',
    description: 'Books: Your Imaginations Greatest Adventure',
}
const BookLayout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default BookLayout