export const metadata = {
    title: 'Magazine | The Universal News',
    description: 'Discover Opportunities.',
  }
const MagazineLayout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default MagazineLayout;