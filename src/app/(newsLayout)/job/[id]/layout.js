export const metadata = {
    title: 'Job | The Universal News',
    description: 'Discover Opportunities: Your Next Career.',
}
const JobLayout = async ({ children }) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default JobLayout;