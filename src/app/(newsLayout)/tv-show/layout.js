export const metadata = {
    title: 'Tv-Show | The Universal News',
    description: 'TV Shows: Entertainment Beyond Boundaries.',
  }
  
const TvShowsLayout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default TvShowsLayout;