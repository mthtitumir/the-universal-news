export const metadata = {
    title: 'Register | The Universal News',
    description: 'Register: Secure Access, Your Gateway.',
  }
  
const RegisterLayout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default RegisterLayout;