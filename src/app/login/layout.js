export const metadata = {
    title: 'Login | The Universal News',
    description: 'Login: Secure Access, Your Gateway.',
  }
  
const LoginLayout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default LoginLayout;